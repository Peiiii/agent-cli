{
  "name": "ai-agent-sdk",
  "version": "1.0.1",
  "description": "SDK for AI Agent CLI plugins",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "clean": "rm -rf dist",
    "patch": "npm version patch",
    "publish-patch": "npm run patch && npm publish"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}
